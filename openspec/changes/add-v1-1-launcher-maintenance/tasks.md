## 1. CLI 动作与帮助

- [x] 1.1 将 `git-pin` 参数解析扩展为 pin、help、list、prune 互斥动作，同时保持 `git-unpin` 的零个或一个参数行为
- [x] 1.2 编写完整 `git pin --help`/`-h` 文本并在访问当前目录、Git 或平台后端前成功返回
- [x] 1.3 增加 CLI 单元测试，覆盖所有合法调用及未知选项、多模式组合、多余参数等用法错误

## 2. 受管启动器发现

- [ ] 2.1 扩展共享启动器契约以逐项返回枚举到的受管入口或带候选路径的解析错误，并更新 fake backend 与现有调用方
- [ ] 2.2 在 Windows 后端复用单项解析逻辑枚举 `.pinned_repo` 下可识别的受管 `.lnk`，忽略外部入口并报告损坏候选
- [ ] 2.3 在 Linux 后端复用单项解析逻辑枚举 `git-pin-*.desktop`，验证受管字段、忽略外部入口并报告损坏候选
- [ ] 2.4 在 macOS 后端复用单项解析逻辑枚举 Git Pin `.app` bundle，验证受管元数据、忽略外部入口并报告损坏候选
- [ ] 2.5 为三个平台增加隔离 fixture 测试，验证精确 inspect 与枚举解析一致、受管目录不存在时为空、外部入口不会进入删除候选

## 3. Repository 有效性自检

- [ ] 3.1 实现不依赖 Visual Studio Code 的 root 自检结果类型，区分有效、可清理的不存在/非目录/非工作树/root 不匹配状态与不可安全清理的检查错误
- [ ] 3.2 复用现有 Git 顶层工作树发现和平台路径等价规则，并增加有效 root、删除后的 root、普通目录、嵌套或不匹配 root 及 Git 调用失败测试
- [ ] 3.3 实现共享扫描编排，将枚举项转为带状态记录、按 repository name 确定排序并聚合逐项错误

## 4. List 与 Prune 编排

- [ ] 4.1 实现 list 输出，包含每项名称、记录 root、有效/无效状态和原因，并为无受管入口输出幂等空结果
- [ ] 4.2 实现 prune 批处理，仅删除经自检确认的僵尸受管启动器，逐项报告删除结果，并保留有效及外部入口
- [ ] 4.3 在 prune 删除前复检待删项以降低扫描竞态风险，并确保复检/删除失败不阻断后续项且不回滚成功删除
- [ ] 4.4 实现批处理错误聚合，使 list/prune 在输出可处理项后对任意枚举、自检或删除失败返回非零并携带名称、root 或候选路径上下文
- [ ] 4.5 将 list/prune 接入应用运行分派，确认两者均不调用 Code 发现逻辑且原有 pin/unpin 输出与行为保持兼容

## 5. 验证与文档

- [ ] 5.1 扩展应用层单元测试，覆盖混合有效/僵尸项、排序、空扫描、Code 缺失、外部项保护、全部清理、无需清理及部分失败继续处理
- [ ] 5.2 扩展跨进程测试，验证 help 的标准输出和零退出、无效参数的标准错误、list 无副作用、prune 幂等及诊断退出码
- [ ] 5.3 更新中英文 README 的 v1.1 用法、list 状态含义、prune 清理边界与示例
- [ ] 5.4 运行格式化、静态检查、完整测试和 OpenSpec 严格验证，并确认三平台 CI 覆盖通过
